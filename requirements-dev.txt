# Development & CI dependencies. Install with:  pip install -r requirements-dev.txt
-r requirements.txt

pytest==9.1.0
ruff==0.9.2

# Notebook / interactive tooling (not needed in production images)
ipykernel==7.2.0
