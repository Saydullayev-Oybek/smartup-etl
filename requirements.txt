# Production runtime dependencies (pinned). Transitive deps are resolved by pip.
# For a fully reproducible lock, compile with pip-tools:  pip-compile requirements.in
pandas==3.0.3
requests==2.34.2
SQLAlchemy==2.0.49
psycopg2-binary==2.9.12
openpyxl==3.1.5
